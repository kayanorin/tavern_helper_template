# 蜃灵 统一渲染 调整计划

> 给 AI 的任务清单。完成顺序按 P0 → P1。每项做完不需要自己打包/验证(工作流自动跑),只需要确保改动符合 Done 标准。

---

## P0 · 音乐播放器(取代原 spatial 描述)

**背景**:`<spatial>` 标签语义已经从"空间位置描述"改成"音乐曲目"。标签名保持不变,但渲染和行为要换。参考实现:`src/蜃灵/原始美化参考.md` L170-L360。

### 任务

- [x] 改写 [src/蜃灵/统一渲染/renderers/top-bar.ts](src/蜃灵/统一渲染/renderers/top-bar.ts)
  - 把 `spatial` 解析成"歌名 + 歌手":正则 `/[《<]([^》>]+)[》>]\s*(.+)/`
  - 解析成功 → 渲染原有 `sl-chip-praesentia` 位置改为音乐 chip(歌名/歌手 + 播放按钮)
  - 解析失败 → 播放按钮 `display:none`,只显示原始文本(降级为普通文字 chip)
  - 关键词 = `歌名 + ' ' + 歌手` 用于查询

- [x] 在 top-bar 渲染产物中包含可用的播放按钮 DOM
  - 推荐:歌词容器加 class `sl-music-text`,按钮加 class `sl-music-btn`,与现有 `sl-chip` 体系共存
  - 按钮 type=button,避免被 form 拦截

- [x] 注入播放器脚本(全局单例)
  - 参考 `原始美化参考.md` L281-L360 的逻辑
  - 用 `window._cosmosAudio` 单例 + `loop = true`,跨消息切换不重叠
  - 音源策略:先 `tryGD`(gdstudio,网易云),catch 后 fallback 到 `tryVKeys`(QQ 音乐)
  - 自动播放被拦截时,按钮文案落回 `▶` 等用户点
  - 注入位置建议:`src/蜃灵/统一渲染/render.ts` 或 `index.ts` 里跟主题切换/折叠脚本同层,只挂一次

- [x] 给所有顶部主题(news/bauhaus/graffiti/古卷书斋顶栏 等)补 chip 的样式
  - 至少:按钮尺寸、`align-items:center`、不要被刊头 grid 撑变形
  - 各主题 extraCss 里如果有针对 `.sl-chip-praesentia` 的特殊样式需要重新审视

### Done 标准

- 写入 `<spatial>《歌名》歌手</spatial>` 时,渲染出音乐 chip,点播按钮可播放
- 写入非音乐格式(任意纯文本)时,渲染为普通文字,无播放按钮且无报错
- 多条消息同时存在播放器时,只有一个 audio 在响

---

## P1 · 公共:展卷 / 收卷 文本切换

**问题**:[bottom-bar.ts:136](src/蜃灵/统一渲染/renderers/bottom-bar.ts#L136) 和 [bottom-bar.ts:216](src/蜃灵/统一渲染/renderers/bottom-bar.ts#L216) 写死了 "展卷",折叠/展开都是同一个字。

### 任务

- [x] 改 [bottom-bar.ts:136 / :216](src/蜃灵/统一渲染/renderers/bottom-bar.ts):把 `<span class="sl-acc-action">展卷</span>` 改成 `<span class="sl-acc-action"></span>`(留空,文本交给 CSS)
- [x] 改 [styles/index.css:263-276](src/蜃灵/统一渲染/styles/index.css#L263-L276):
  - `.sl-acc-action::before { content: "展卷"; }`
  - 已有的 `.sl-acc[open] .sl-acc-action, .sl-memory-details[open] .sl-acc-action` 块里加 `::before { content: "收卷"; }`
- [x] 检查各主题 extraCss 里有没有覆盖 `.sl-acc-action` 文本相关样式,确保 `::before` 不被遮挡

### Done 标准

- 所有主题(古卷书斋/报纸/包豪斯/涂鸦)下,折叠时显示"展卷",展开时显示"收卷"

---

## P1 · 报纸主题(news / news-deep)

### 任务

- [x] **PC 端 · 小总结 block 长短不一** [news-extra-css.ts:398-447](src/蜃灵/统一渲染/themes/news-extra-css.ts#L398-L447)
  - 将 `.sl-memory-body` 从 `grid auto-fit` 改成 CSS multi-column:`column-count: 3; column-gap: 16px; column-rule: 1px solid var(--sl-border-thin);`
  - `.sl-block` 加 `break-inside: avoid; display: block; margin: 0 0 14px;`,去掉 `margin-bottom: 0`
  - 现有 `@media (max-width: 760px)` 里 `grid-template-columns: 1fr` 改成 `column-count: 1`
  - 中间断点(如 1100px 以下)降为 `column-count: 2`,只动 PC 端,手机端继续单列
  - 首字下沉(`.sl-block-*::first-letter`)在 column 容器内表现良好,保留即可

- [x] **窄屏 · 顶栏标签布局**(`<spatial>` 改成音乐后只剩 Tempus/Locus/Caelum/Dramatis 等)
  - 当前 [news-extra-css.ts:192-209](src/蜃灵/统一渲染/themes/news-extra-css.ts#L192-L209) 已有窄屏块,确认 Tempus 等 chip 的标签和值在窄屏是上下布局,如有横排紧贴问题修正

- [x] **窄屏 · 底部角色状态栏标签布局** [news-extra-css.ts:330-343](src/蜃灵/统一渲染/themes/news-extra-css.ts#L330-L343)
  - 窄屏(`@media (max-width: 760px)`)内增加 `.sl-acc-body .sl-detail` 的 `grid-template-columns: 1fr`,标签 `.sl-key` 改为块级、`border-bottom` 保留以视觉分隔
  - 目标样式:`Relation` / `Outfit` / `Posture` 标签独占一行,内容换行显示在下方

### Done 标准

- PC 端打开报纸主题,小总结 6 个 block 内容长短不一时,不再有一列底部留大段空白;视觉像报纸
- 手机端打开报纸主题,顶部 chip / 底部角色字段都是"标签在上、内容在下"

---

## P1 · 古卷书斋(gjsz / gjsz-deep)

**背景**:这两个主题没有 extraCss,完全吃 [styles/index.css](src/蜃灵/统一渲染/styles/index.css) 默认的 `.sl-detail` 等基础布局。问题集中在 `.sl-detail` 的 grid 上。

### 任务

- [x] **PC 端 · 标签文字紧贴边缘 + 居中不齐** [styles/index.css:282-300 附近](src/蜃灵/统一渲染/styles/index.css#L282)(自行确认行号)
  - `.sl-detail` / `.sl-acc-body .sl-detail` 中,`.sl-key` 增加内边距(如 `padding: 2px 8px`)避免文字贴边
  - 调整 `align-items: center` 或 `align-self: center`,让标签框和内容垂直居中
  - 如果觉得 grid 不够优雅,可以改成:标签框固定宽 + 居中对齐内文字 + 内容左对齐;或直接采用下面的窄屏方案统一做成"上下排布"

- [x] **窄屏 · 横向标签导致空白 / 标签框被撑大**
  - 默认 `.sl-detail` 是 `grid-template-columns: 84px 1fr` 之类的横向布局,在窄屏改成单列(`grid-template-columns: 1fr` 或 `display: block`),标签作为块级独占一行
  - 同样处理顶栏的 `.sl-row-meta` chip(如果存在紧贴问题)
  - 注意:这套改动作用域应限定在没有自定义 extraCss 的"基础样式"上,避免影响已经定制好的 news/bauhaus/graffiti
  - 实现思路:在 `styles/index.css` 的 `@media (max-width: 760px)` 中改默认布局,各定制主题如已经有覆盖则不受影响

### Done 标准

- PC 端古卷书斋的 Relation/Outfit/Posture 等字段,标签框居中、文字不贴边
- 窄屏古卷书斋下,所有标签字段为"标签独占一行 + 内容在下"

---

## P1 · 包豪斯(bauhaus)

PC 端无修改。

### 任务

- [x] **窄屏 · 底部角色状态栏标签布局** [bauhaus-extra-css.ts:350-365 附近](src/蜃灵/统一渲染/themes/bauhaus-extra-css.ts#L350)
  - 在 bauhaus 的 `@media (max-width: 760px)`(若无则新增)中,将 `.sl-acc-body .sl-detail` 改为单列、`.sl-key` 块级
  - 目标:outfit / Relation / Posture 在窄屏改为上下布局

- [x] **窄屏 · 选择支(branches)Option 文案换行**
  - [bauhaus-extra-css.ts:532+](src/蜃灵/统一渲染/themes/bauhaus-extra-css.ts#L532) 中 `.sl-branch-btn` 是 grid `auto 1fr auto`,Option 在窄屏会和"你的选择是……"挤在一起换行
  - 两种方案任选(优先方案 A):
    - **方案 A**:窄屏改成单列(`grid-template-columns: 1fr`),A/B/C key 行 + 文本行 + 箭头行依次堆叠;或 key 与文字同一行、子标题独占下一行
    - **方案 B**:窄屏调小相关字号(`.sl-branch-text` / 子标题),让横排不挤
  - 实施后确保选项块整体观感不破坏包豪斯几何风格

### Done 标准

- 窄屏 outfit 等字段为上下布局
- 窄屏选项块不再有"Option 文字硬换行 / 字符挤压"现象

---

## P1 · 涂鸦街头(graffiti)

### 任务

- [x] **PC 端 · 小总结 block 长短不一** [graffiti-extra-css.ts 中 `.sl-memory-body` 区块](src/蜃灵/统一渲染/themes/graffiti-extra-css.ts)(自行 grep `sl-memory-body` 定位)
  - 同报纸方案:`.sl-memory-body` 改 multi-column;`.sl-block` 加 `break-inside: avoid`
  - **特别注意**:graffiti 的 `.sl-block` 有 `transform: rotate(...)` 等 hover 动效,以及 `box-shadow` 偏移,需要给每个 block 留出 hover 时的位移空间
    - `margin` 需比报纸主题更大(建议 `margin: 6px 4px 18px;` 视觉测试调节),避免 hover 时位移撞到下一列或被裁切
    - 必要时 `padding` 或 `column-gap` 增大,确保 box-shadow 完整显示
  - 同样响应式:中等宽度降 2 列,窄屏 1 列

- [x] **窄屏 · 底部角色状态栏标签布局** [graffiti-extra-css.ts:362-378 附近](src/蜃灵/统一渲染/themes/graffiti-extra-css.ts#L362)
  - 同包豪斯方案:窄屏 `.sl-acc-body .sl-detail` 单列、`.sl-key` 块级

- [x] **窄屏 · 选择支 Option 文案换行** [graffiti-extra-css.ts:604+](src/蜃灵/统一渲染/themes/graffiti-extra-css.ts#L604)
  - 同包豪斯方案 A 或 B
  - 注意保留 `transform: rotate(±0.5deg)` 等装饰效果

### Done 标准

- PC 端涂鸦的 6 个 block 填充饱满,hover 动效不被相邻 block 遮挡或裁切
- 窄屏涂鸦下,outfit 等为上下布局,选项块无挤压换行

---

## 一些公共注意点

- 改 CSS 时优先在已有 `@media (max-width: 760px)` 块内追加,而非新建块,避免断点不一致
- multi-column 模式下不要给 `.sl-block` 加 `display: inline-block` 之外的不兼容值,首字下沉 `::first-letter` 在 column 中工作正常
- 所有窄屏标签改"上下"时,`grid-template-columns: 1fr` 比 `display: block` 更稳(保留 grid 的 gap)
- 改动避免引入新的全局 class,尽量复用现有 `sl-*` 命名
- 不要为旧 spatial 语义保留兼容代码 — 直接替换为音乐播放器

---

<!-- ============ 第二轮(P2):基于实际渲染反馈的修补 ============ -->
<!-- 第一轮(P0/P1)上线后用户反馈了以下问题,集中修复。完成顺序按编号即可。 -->

## P2-1 · `<spatial>` 音乐 chip 显示完整歌词

**问题**:当前只渲染了 `《歌名》歌手` 这一段,但 LLM 实际输出格式是 `歌词文本 —— 《歌名》歌手`,歌词部分被丢弃。

**输入示例**(预期):

```text
<spatial>夜色中你的回望像一封未寄出的信 —— 《信》张悬</spatial>
```

- 歌词:`夜色中你的回望像一封未寄出的信`
- 歌名:`信`
- 歌手:`张悬`
- 关键词(查询用):`信 张悬`

### 任务

- [x] 修改 [top-bar.ts](src/蜃灵/统一渲染/renderers/top-bar.ts) 中 `MUSIC_RE` 与 `parseMusic`,新增"歌词"字段
  - 建议正则:`/^(.*?)\s*[—\-–]{1,}\s*[《<]([^》>]+)[》>]\s*(.+)$/s`,捕获组 1=歌词、2=歌名、3=歌手;歌词允许为空(`歌词` 段为空白时降级到原行为)
  - 兼容旧格式 `《歌名》歌手`(无破折号):走当前正则,歌词为空字符串
- [x] `renderMusicChip` 输出结构调整(歌词 + 歌名/歌手分两行):

  ```html
  <div class="sl-chip sl-chip-praesentia sl-music-chip">
    <span class="sl-key">Praesentia</span>
    <div class="sl-music-text">
      <div class="sl-music-lyric">{歌词,可空}</div>
      <div class="sl-music-meta">《{歌名}》— {歌手}</div>
    </div>
    <button type="button" class="sl-music-btn" data-sl-music-keyword="..." ...>▶</button>
  </div>
  ```

  - 若无歌词,`.sl-music-lyric` 不渲染(避免空行)
- [x] 关键词仍是 `${歌名} ${歌手}`,歌词不进入查询
- [x] [styles/index.css](src/蜃灵/统一渲染/styles/index.css) `.sl-music-text` 改为支持多行布局(`display: flex; flex-direction: column; gap: 2px;`),`.sl-music-lyric` 字号略小斜体,`.sl-music-meta` 主信息略大
- [x] 各主题 (`news` / `bauhaus` / `graffiti`) 的 `.sl-chip-praesentia` 区段需要回看是否对 `.sl-val` 的 `display: block; text-align: justify; ::first-letter` 等规则影响新结构;若需要为 `.sl-music-text` 取消首字下沉/justify,加针对性覆盖

### Done 标准

- 写入 `歌词 —— 《歌名》歌手` 时,chip 同时显示歌词与歌名/歌手,搜歌仍按 `歌名 歌手`
- 写入纯 `《歌名》歌手`(无歌词)时,只显示歌名/歌手行,无空白歌词行
- 写入纯文本(无法解析)时,降级为单行文字 + 按钮隐藏(同当前行为)

---

## P2-2 · 包豪斯 / 涂鸦 Branch 顶部"OPTION"大标题在窄屏字符级折行

**问题**:截图中包豪斯 `OPTION` 标签被压成 `O P T I O / N` 竖向折行,旁边 `你的选择是……` 也被挤换行。涂鸦同位置预计有同样问题。

**根因**:

- `.sl-card-head` 设 `flex-wrap: nowrap`,强制 title 与 sub 在同一行
- `.sl-card-title` 是 uppercase + 大字号 + letter-spacing + 黑色色块,窄屏宽度不够触发字符内折行

### 任务

- [x] 在 [bauhaus-extra-css.ts](src/蜃灵/统一渲染/themes/bauhaus-extra-css.ts) 的 `BAUHAUS_BRANCH_EXTRA_CSS` 已有 `@media (max-width: 760px)` 块(若无则新增)内追加:
  - `& .sl-card-head { flex-wrap: wrap; gap: 6px; }`
  - `& .sl-card-title { font-size: 1em; letter-spacing: 0.08em; padding: 3px 10px; white-space: nowrap; }`
  - `& .sl-card-sub { font-size: 0.7em; letter-spacing: 0.08em; }`
- [x] 同样改 [graffiti-extra-css.ts](src/蜃灵/统一渲染/themes/graffiti-extra-css.ts) 的 `GRAFFITI_BRANCH_EXTRA_CSS` 已有 `@media (max-width: 760px)` 块内追加:
  - `& .sl-card-head { flex-wrap: wrap; gap: 6px; }`
  - `& .sl-card-title { font-size: 1.1em; letter-spacing: 0.12em; transform: rotate(-1deg); white-space: nowrap; }`
  - `& .sl-card-sub { font-size: 0.7em; }`
- [x] 顺便检查报纸主题(news / news-deep)的 branch 在窄屏是否也有挤压,如有同步修

### Done 标准

- 窄屏下包豪斯/涂鸦的 Option 卡:`OPTION` 作为一整段(不再字符级折行),`你的选择是……` 完整显示在下一行或同行右侧
- 不破坏 PC 端原有视觉

---

## P2-3 · 包豪斯角色状态栏标签 (Outfit/Relation/Posture) 改为居中对齐

**问题**:截图显示 `OUTFIT` 标签框与右侧内容文本(`月白色立领旗袍,外披灰色羊绒披肩`)是"上沿对齐"(label 顶 ≡ 内容顶),内容多行时下沉,视觉错位。

**根因**:[bauhaus-extra-css.ts](src/蜃灵/统一渲染/themes/bauhaus-extra-css.ts) 中 `.sl-acc-body .sl-detail .sl-key { align-self: start; }`。

### 任务

- [x] 把 bauhaus 的 `& .sl-acc-body .sl-detail .sl-key` 中 `align-self: start;` 改为 `align-self: center;`
- [x] 同时在 `& .sl-acc-body .sl-detail` 上加 `align-items: center;`(让 .sl-val 也居中,以防 val 是单行而 key 居中显得不对齐)
- [x] 顺手审 news / graffiti 同位置:
  - news `& .sl-acc-body .sl-detail .sl-key { align-self: start; }` → 也改 center?(视觉决定,如果"标题报纸感"需要 start 就保留并在计划中标注例外)
  - graffiti `& .sl-acc-body .sl-detail .sl-key { align-self: start; }` → 同上
- [x] 若多行内容仍显得偏移,可叠加 `.sl-acc-body .sl-detail { row-gap: 2px; }` 或微调 `.sl-key` 的 `min-height`

### Done 标准

- 包豪斯下 `Outfit` / `Relation` / `Posture` 标签框垂直中线与内容文本垂直中线对齐,无论内容是单行还是多行
- 单独/同步处理 news/graffiti 后,各主题统一无错位感

---

## P2-4 · 涂鸦小总结 block 右上角"喷漆双竖线"在 multi-column 下错位

**问题**:第一轮把涂鸦的 `.sl-memory-body` 改成 `column-count: 3` 后,每个 block 右上角 `::before` / `::after` 形成的双竖线视觉上"漂"到了相邻位置(不再稳定附着在所属 block)。

**可能成因**:

- `top: -10px` 让伪元素伸出 block 上沿,在多列容器内可能被相邻列的 block 视觉混入
- 多列环境下,如果块跨列分裂(理论上 break-inside:avoid 已禁止)或绝对定位在 column fragment 上的解释存在浏览器差异
- box-shadow / hover transform 同时存在,叠加显示混乱

### 任务

- [x] 把 [graffiti-extra-css.ts](src/蜃灵/统一渲染/themes/graffiti-extra-css.ts) 中 `.sl-block::before` / `.sl-block::after` 的 `top: -10px` 改为 `top: 0`(或 `top: 4px`),让双竖线落在 block 内部右上角
- [x] 相应调整 `height`:静置 `before { height: 14px } after { height: 18px }`,hover `:hover::before { height: 30px } :hover::after { height: 38px }` 保留视觉伸长
- [x] 验证多列下,每个 block 的双竖线都在自己的 border 内,与相邻列 block 隔离;若仍漂移,改用 inset border(右上角描边)替代竖线
- [x] 顺便检查 `.sl-block` 的 `overflow: visible` 是否需要在 column 模式下改为 `overflow: clip`(只裁切超出部分,不影响 hover transform 自身)

### Done 标准

- multi-column 三列布局下,每个 block 的右上角双竖线稳定附着在 block 内部右上角,不再"飘"到其他位置
- hover 时双竖线伸长保留视觉效果,不溢出到相邻列

---

## P2-5 · `<list>` 按角色拆分子块 + 自适应排版

**问题**:`<memory><list>` 中常包含多个角色各自的 TODO,目前全部塞在同一个 `.sl-block-list` 的 body 里(`nl2br` 后整段堆叠),阅读不友好。

**约定输入格式**(择一固定,推荐方案 A):

- **方案 A**(方括号头,推荐):

  ```text
  [角色名 A]
  - todo 1
  - todo 2
  [角色名 B]
  - todo 3
  ```

- **方案 B**(冒号头):

  ```text
  角色名 A:
    - todo 1
    - todo 2
  角色名 B:
    - todo 3
  ```

无法识别角色名时回退为单段原样显示。

### 任务

- [x] 在 [bottom-bar.ts](src/蜃灵/统一渲染/renderers/bottom-bar.ts) 增加 `parseListByCharacter(raw: string): Array<{ name: string; items: string[] }>` 函数
  - 输入是 `<list>...</list>` 内的纯文本(已 trim)
  - 按选定方案识别角色头,把后续以 `-` / `*` / `•` 开头(或缩进)的行归到该角色 items
  - 解析得 0 个角色 → 返回 `[{ name: '', items: [整段文本] }]`,在渲染端识别为"无拆分,原样渲染"
- [x] 修改 `renderBottomBar` 中 list 渲染段(目前是 `<div class="sl-block sl-block-list">...nl2br(list)...`)为:

  ```html
  <div class="sl-block sl-block-list">
    <div class="sl-block-title">List</div>
    <div class="sl-list-grid">
      <div class="sl-list-char">
        <div class="sl-list-char-name">{角色名}</div>
        <ul class="sl-list-char-items">
          <li>{item 1}</li>
          <li>{item 2}</li>
        </ul>
      </div>
      <!-- 重复 -->
    </div>
  </div>
  ```

  - 无角色名(回退) → 不输出 `.sl-list-char-name`,只渲染单一 `.sl-list-char` 包裹 `<div>{nl2br 文本}</div>`
- [x] 在 [styles/index.css](src/蜃灵/统一渲染/styles/index.css) 加基础样式
- [x] 各主题适配:
  - **news / news-deep**:multi-column 下 `.sl-block-list` 内的 `.sl-list-grid` 改 `column-count: 1` 或干脆让 `.sl-block-list { column-span: all }` 占据全行,避免 mini-block 被列再切一次
  - **bauhaus**:`.sl-list-char` 加色块标题(nth-child 4 色轮换),保持几何感
  - **graffiti**:`.sl-list-char` 加 `transform: rotate(±0.5deg)`,背景色随 nth-child 切换,贴纸感
  - **古卷书斋(gjsz)**:沿用默认 + 字体衬线匹配
- [x] 单角色或解析失败时确认无视觉差异(只是不出现 .sl-list-char-name)

### Done 标准

- 输入 list 含多角色时,每个角色独立显示为一个 mini-block,横向自适应铺排,窄屏单列
- 输入 list 无法解析角色时,保留旧的整段显示行为,无报错
- 报纸/包豪斯/涂鸦三个定制主题下 mini-block 样式与主题协调,无破坏

---

## P2-6 · 古卷书斋 `.sl-key` 标签改为"左右修饰线" (变体 A2)

**参考**:[src/蜃灵/古卷书斋修改方案.txt](src/蜃灵/古卷书斋修改方案.txt) 中变体 **A2 左右修饰线(杂志排版)**:

```css
.style-3a2 .label {
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.style-3a2 .label::before,
.style-3a2 .label::after {
  content: "";
  display: block;
  width: 20px;
  height: 1px;
  background-color: #A69785;
}
```

**作用范围**:**仅** gjsz / gjsz-deep 两个主题,不影响 news / bauhaus / graffiti。

### 任务

- [x] 在 [styles/index.css](src/蜃灵/统一渲染/styles/index.css) 中已有的 gjsz 专属 selector 块附近(第一轮已经写过 `[data-theme-id="gjsz"]` 这种选择器,直接续写)新增 A2 杂志风修饰线
- [x] 与第一轮添加的 gjsz `.sl-detail .sl-key { display: flex; justify-content: center; width: 100%; padding: 4px 9px; }` **冲突**:把那段改为 A2 风格(`display: inline-flex; width: auto; padding: 2px 0; justify-content: flex-start;`),并把"标签居中"的需求改为通过 grid `align-items: center` + `.sl-detail { justify-items: start | center }` 实现
- [x] 窄屏下 `::before` / `::after` 的 `width` 缩到 12px,避免标签自身把行挤爆;或在 `max-width: 480px` 时隐藏两侧细线(`display: none`),只留文字
- [x] 检查 gjsz 顶栏(`.sl-row-meta .sl-chip` 等)的 .sl-key 是否需要 wrap-friendly 处理(A2 让 key 变 inline-flex,与现有 baseline flex 兼容)

### Done 标准

- 古卷书斋(浅/深)主题下,所有 .sl-key 标签(Tempus/Locus/Caelum/Praesentia/Relation/Outfit/Posture/Worldstate/CurrentTask/Plot/Psychology/List/Database 等)显示为"细线 — 文字 — 细线"杂志风
- 不影响 news / bauhaus / graffiti
- 窄屏下不会因为左右细线把行挤换行(必要时缩短细线或隐藏)

---

## P2 公共注意点

- 第二轮的改动同样优先在已有 `@media (max-width: 760px)` 块内追加
- P2-1(歌词)与 P2-6(A2 标签)都会改 `.sl-music-text` 或 `.sl-key` 的 layout,做之前先确认彼此不冲突
- P2-3(标签居中)如果只想动 bauhaus,可只改 bauhaus extraCss;若觉得 news/graffiti 视觉也错位,统一一次性改掉
- P2-5(list 拆分)是这一轮唯一动 TS 逻辑的,其他都是 CSS 调整,优先做 CSS 任务再合并 TS 改动
